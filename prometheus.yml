global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'pushgateway'
    honor_labels: true # Preserves the metric signatures sent by your script
    static_configs:
      - targets: ['pushgateway:9091']